# random-cat-image-app
Random Cat Image App is a simple Flutter application that fetches and displays random cat images from The Cat API each time the user taps a button. It demonstrates API integration, asynchronous data handling, UI state updates, and clean widget structuring in Flutter.
